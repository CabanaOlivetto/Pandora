dialogo = [];           // Será preenchido pelo NPC (obj_vulto)
pagina = 0;             // Qual frase do diálogo está ativa no momento
caractere_atual = 0;    // Controla o efeito de digitar letra por letra
velocidade_texto = 0.5; // Velocidade da digitação (maior = mais rápido)